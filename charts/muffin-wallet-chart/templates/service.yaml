apiVersion: v1
kind: Service
metadata:
  name: muffin-wallet
  labels:
    app: muffin-wallet
spec:
  type: {{ .Values.service.type }}
  selector:
    app: muffin-wallet
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}