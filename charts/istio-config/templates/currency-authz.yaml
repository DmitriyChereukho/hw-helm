apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-wallet-only
  namespace: muffin
spec:
  selector:
    matchLabels:
      app: muffin-currency
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/muffin/sa/default
